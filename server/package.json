{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "start": "node -r dotenv/config lib/server/src/index.js",
    "deploy": "node -e 'console.error(`Not supported`)'",
    "clean": "rm -rf ./lib"
  },
  "engines": {
    "node": "12"
  },
  "main": "lib/server/src/index.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "firebase": "^7.14.5",
    "firebase-admin": "^8.12.1",
    "fireorm": "^0.13.4",
    "reflect-metadata": "^0.1.13"
  },
  "private": true
}
